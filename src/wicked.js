
import Transition from "./wicked-transition";
import Scene from "./wicked-scene";

export class Wicked {
  constructor() {
    this.transitions = [];
  }

  addTransition(transition) {

  }

  findPath(start, end) {

  }

  /**
   * Returns an array of transitions necessary to go from point A to point B
   */
  _findPath(start, end, path) {

  }
}

const wicked = new Wicked();

export default wicked;
